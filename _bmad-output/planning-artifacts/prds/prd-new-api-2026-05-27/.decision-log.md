# Decision Log — `{NEW_PRODUCT_NAME}` V1 企业管控基线 PRD

> 用于记录 PRD 创建、范围裁剪、假设、覆盖和后续变更。所有条目按时间顺序追加。

---

## 2026-05-27 — PRD 创建

**触发：** 用户调用 `bmad-prd`，承接已 approved 的产品简报，进入 PRD 阶段。

**输入：**
- `_bmad-output/planning-artifacts/briefs/brief-new-api-2026-05-27/brief.md`
- `_bmad-output/planning-artifacts/briefs/brief-new-api-2026-05-27/addendum.md`
- `_bmad-output/planning-artifacts/briefs/brief-new-api-2026-05-27/.decision-log.md`
- `AGENTS.md`
- `pkg/billingexpr/expr.md`
- 棕地代码抽查：`model/user.go`、`model/log.go`、`model/usedata.go`、`controller/custom_oauth.go`、`router/api-router.go`

**工作模式：** Fast path。用户未要求逐节共创，故按简报直接起草 V1 PRD，并用开放问题保留需要用户确认的决策。

**范围决策：**
- 仅创建 V1「企业管控基线」PRD。
- V2「代码库 DeepWiki / WebChat / 多模态」不进入本 PRD。
- V3「SaaS 多租户 / 服务化」不进入本 PRD。
- 本地组织管理 UI 不默认进入 V1，列为开放问题。

**关键假设：**
- V1 是单企业自部署实例，不提供多企业运行时隔离。
- V1 可以预留 `tenant_id`，但默认固定为单租户值。
- V1 定期报告优先复用现有邮件能力；钉钉机器人/Webhook 是否必选待确认。

**开放问题：**
- 产品名何时替换 `{NEW_PRODUCT_NAME}`。
- 无钉钉企业是否需要 V1 本地组织管理 UI。
- `tenant_id` 是否扩展到旧表。
- 钉钉机器人/Webhook 是否 V1 必选。
- 钉钉登录复用 `CustomOAuthProvider` 还是专用 provider。
- 部门配额周期口径。
- 多部门员工的主部门规则。

**输出：**
- `prd.md`
- `addendum.md`
- `.decision-log.md`

## 2026-05-27 — 机械自检

**检查项：**
- FR 编号连续：FR-1 至 FR-20。
- UJ 编号连续：UJ-1 至 UJ-4。
- SM 编号连续：SM-1 至 SM-5，含 SM-C1 至 SM-C3 counter-metrics。
- `[ASSUMPTION]` 标签已在假设索引列出。
- 开放问题 OQ-1 至 OQ-7 已集中在 §13。

**修正：**
- 修正 `prd.md` frontmatter 中 `source_brief` 与 `source_addendum` 的相对路径，从 PRD 工作目录正确指向 briefs 工作目录。

## 2026-05-27 — Validate 后的 Update（rubric walker findings 滚回）

**触发：** 用户在 Validate 后选择"滚 Update"。Rubric walker 综合等级 Poor（critical=1, high=3, medium=4, low=3）；critical 为 FR-10 部门配额并发安全无可执行验证命题。

**变更：**

- **FR-10（critical）：** 引入"扣减时机（V1 验收基线）"小节，区分预消费 / 实际结算 / 失败回退三条路径；可验证结果改为 ≥50 并发、同部门 ≥10 用户、≥10 分钟、终态偏差 0、明确错误码 `department_quota_exhausted`。新增 ASSUMPTION：V1 预消费先扣部门额度，并发安全机制由架构确定。
- **FR-6（high）：** 新增"主部门读取顺序"小节，登录主部门取自最近一次成功同步快照，钉钉本次回调仅做校验或异步增量；同步进行中不阻塞登录；无快照时主部门为"未归属"且登录可完成。
- **FR-19（high）：** 新增"重试策略（V1 默认）"小节，3 次指数退避（30s / 2m / 10m），最终失败可后台一键重发；同事件 5 分钟内聚合避免噪声。
- **OQ-3（high）：** 在 OQ-3 下新增 A/B 两条路径的代价对照表（V1 工期、迁移与索引改造、SaaS 切换代价）；V1 默认选 A，选 B 需要 V3 立项与额外工期预算。
- **FR-5（medium）：** 列出 V1 至少返回的 5 类连通性测试错误码 / 标识。
- **FR-12（medium）：** 接近耗尽阈值改为"预警 80% / 紧急 95%"两档默认，可调整并校验顺序。
- **FR-15（medium）：** CSV 字段顺序固定，包含"未归属"行，行排序与页面一致，空值统一空字符串；导出日志含筛选条件快照与行数。
- **FR-20（medium）：** 给出风险率公式（分子分母均含被拦截请求；未归属请求单独显示）。
- **OQ-6 / OQ-7（medium）：** 标记为 phase-blocker，进入 epic/story 前必须关闭。
- **§8 NFR 性能（low）：** 加入"100 万 logs / ≤100 部门下 30 天看板 < 3 s P95"基线，并标 ASSUMPTION 可由架构阶段调整。
- **§12 发布与验收（low）：** 写入"V1 目标交付窗口约 6-8 周（来自简报）"。
- **§14 假设索引：** 新增 FR-10 与 §8 NFR 两条 ASSUMPTION，索引由 3 条增至 5 条。

**未变更：**

- 5 项 strong 维度（Substance / Strategic coherence / Scope honesty / Downstream usability / Shape fit）所属 FR、SM、UJ、术语表内容未改。
- FR 编号、UJ 编号、SM 编号、OQ 编号仍连续；本次更新未新增编号。

**Phase-blocker 状态：**

- OQ-6（部门配额周期）、OQ-7（多部门员工主部门规则）— 进入 epic/story 前必须关闭，决策权在 PM + IT 林工。
- OQ-2、OQ-3、OQ-4 — 非阻塞，可推迟到架构阶段；OQ-3 现在带可比较的代价表。
- OQ-1（产品名）、OQ-5（OAuth provider 选择）— 可在架构阶段处理。

**输出：**

- 更新 `prd.md`（§4.1 FR-4 / §4.2 FR-5,FR-6 / §4.3 FR-10,FR-12 / §4.4 FR-15 / §4.5 FR-19,FR-20 / §8 / §12 / §13 OQ-3,OQ-6,OQ-7 / §14）。
- 保留本次 Validate 产出：`validation-report.html`、`validation-report.md`、`review-rubric.md`。
